<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.studies.se">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Day">
        <activity
            android:name=".EmployeeStatistic"
            android:label="@string/title_activity_worker_statistic"
            android:theme="@style/Theme.Day.NoActionBar"></activity>
        <activity android:name=".EditEmployee" />
        <activity android:name=".EmployeeStatus" />
        <activity android:name=".EmployeeInfo" />
        <activity android:name=".AddEmployee" />
        <activity android:name=".Employees" />
        <activity android:name=".MyStations" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>